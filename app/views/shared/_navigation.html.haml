= link_to "ComicBookApp", root_path, :class => 'brand'
%ul.nav
  %li= link_to "Archive", comics_path
  - if signed_in? and current_user.admin
    %li= link_to "Manage Comics", manage_comics_path
    -#%li= link_to "Manage Series", manage_series_path
  - if signed_in?
    %li
      = link_to('Edit account', edit_profile_path)
  - else
    %li
      = link_to('Sign up', sign_up_path)
  - if signed_in?
    %li
      = link_to('Logout', sign_out_path, :method=>'delete')
  - else
    %li
      = link_to('Sign in',sign_in_path)
